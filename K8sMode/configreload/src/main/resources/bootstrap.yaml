spring:
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 5000
      config:
        name: ${spring.application.name}
        namespace: default
        sources:
          # Spring Cloud Kubernetes looks up a ConfigMap named my-another-config in namespace default-namespace
          - name: my-another-config
          # Spring Cloud Kubernetes looks up a ConfigMap named  ${spring.application.name} in whatever namespace n2
          #- namespace: n2
          # Spring Cloud Kubernetes looks up a ConfigMap named c3 in namespace n3
          #- namespace: n3
          #  name: c3