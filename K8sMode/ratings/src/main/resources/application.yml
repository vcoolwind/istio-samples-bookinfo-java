spring:
  application:
    name: ratings
  datasource:
  url: jdbc:h2:mem:products;DB_CLOSE_ON_EXIT=FALSE
  username: sa
  password:
  driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
server:
  port: 8101
